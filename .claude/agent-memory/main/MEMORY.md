- [User background & collaboration style](user_background_and_collaboration.md) — career BASIC programmer, learning-mode user, wants visibility not autonomy
- [DRAW compile convention](feedback_draw_compile_convention.md) — foreground + 600000ms timeout for every qb64pe build, never background
- [DRAW observability logs](reference_draw_activity_log.md) — `.claude/activity.log` (every Bash/Agent call) + `~/.claude/make.log` (qb64pe compile output)
- [qb64pe sandbox limitation](reference_qb64pe_sandbox_limitation.md) — silent fail under sandbox ("Press enter to continue" with no banner); retry with `dangerouslyDisableSandbox: true` and it compiles cleanly
- [qb64pe compile output capture](feedback_qb64pe_compile_output_capture.md) — direct CLI compile masks errors with "Press enter to continue"; use MCP `compile_and_verify_qb64pe` first, treat SIGTERM as "still running" not "failure", check `DRAW.run` existence for true success
- [VSCode task for DRAW build](feedback_vscode_task_for_draw_build.md) — user-canonical build is VSCode F5 "EXECUTE: Run" (chains BUILD: Remove → BUILD: Compile). Use that command, ask user to F5+paste output when bash hides errors, NEVER inspect `qb64pe/internal/temp/`
- [Sandbox dotfile pollution](feedback_sandbox_dotfile_pollution.md) — Claude Code sandboxed Bash creates husk dotfiles (.bashrc/.zshrc/.idea/etc.) in cwd on every invocation. Prefix Bash with `cd "$TMPDIR" && ` + use absolute paths to avoid project-root pollution. User has explicitly rejected gitignore as a fix.
- [QB64-PE wiki first](feedback_qb64pe_wiki_first.md) — MCP scraped wiki drops syntax/params content; WebFetch the wiki URL directly for graphics keywords
- [Isolated tests first](feedback_isolated_tests_first.md) — for risky QB64-PE quirks, build a minimal `DEV/EXPERIMENTS/*.BAS` repro before touching DRAW source
- [Trust user symptoms](feedback_trust_user_symptoms.md) — echo the user's exact symptom words; don't silently reinterpret "partial paint" as "alpha trails"
- [Tag before experiments](feedback_tag_before_experiments.md) — tag known-good state before exploration; tags survive squash-merges and enable surgical recovery
- [Memory location](feedback_memory_location.md) — ALWAYS write DRAW memory to `.claude/agent-memory/main/` (version-controlled), NOT to `~/.claude/projects/...` (per-user)
- [Destructive in-place transforms](feedback_destructive_in_place_transforms.md) — new transform features (scale/flip/rotate) modify the layer in place; bundle layer + selection undo with HISTORY group; scale wand mask alongside content
- [Selection > brush > layer priority](feedback_selection_vs_brush_priority.md) — hotkey dispatch order when multiple targets are valid; require both ACTIVE flag and valid IMAGE handle for brush
- [Hotkey grep sweep before adding chords](feedback_hotkey_grep_sweep.md) — DRAW has no central dispatch; grep _KEYDOWN(<keycode>) and CASE "letter" across whole KEYBOARD.BM before adding any new chord (NOTE: superseded by input rearchitecture once Phase 2+ complete)
- [Input system reference](reference_input_system.md) — INPUT/INPUT.BI/BM unified dispatch; CORE/HELPERS.BI/BM utilities; migration phase status; dev mode via --developer + inputs.log; spec in PLANS/REARCHITECTURE.md
- [Self-blocking context bug](feedback_self_blocking_context_bug.md) — chord-initiator tool keys (M/Z/E/F/W) must NOT forbid their own CTX_*_HELD bit; the context update runs before dispatch and would self-block on the very press edge
- [QB64-PE NOT is bitwise](feedback_qb64pe_not_is_bitwise.md) — `NOT 1` returns `-2` (truthy), not `0` (falsy). For non-boolean flags use `IF flag = 0 THEN` instead of `IF NOT flag THEN`. The naive `NOT` only works on values that are exactly 0 or -1.
- [QB64-PE $DYNAMIC + DIM SHARED doesn't allocate](feedback_qb64pe_dynamic_dim_shared.md) — under `$DYNAMIC`, module-level `DIM SHARED arr(0 TO N)` reserves the symbol but doesn't allocate. First read crashes "Subscript out of range". `ERASE` actively deallocates. Always init in `_init` via a fill loop; never `ERASE` a SHARED array you want to keep using.
- [QB64_GJ_LIB submodule isolation](feedback_qb64_gj_lib_no_consumer_helpers.md) — `includes/QB64_GJ_LIB/` is a vendored submodule shared with other apps. Never call DRAW-side helpers (`SAFE_FREEIMAGE`, `INVALIDATE_scene`, `MODS_NOW%`, etc.) from inside the lib — only QB64-PE built-ins and `QB64_GJ_*`-prefixed helpers. Add new lib helpers to `_GJ_LIB_COMMON.BI`.
- [QB64-PE no DECLARE SUB/FUNCTION](feedback_qb64pe_no_declare_sub_function.md) — QB64-PE auto-discovers SUBs/FUNCTIONs from the include chain. Do NOT add `DECLARE SUB`/`DECLARE FUNCTION` to DRAW code. ONLY `DECLARE LIBRARY` / `DECLARE DYNAMIC LIBRARY` / `DECLARE STATIC LIBRARY` (FFI) are still required. Submodule lib code has its own conventions — don't strip its DECLAREs.
- [Never leave unused variables](feedback_never_leave_unused_variables.md) — every QB64-PE `warning: Unused variable` is a defect. Delete local DIMs; for unused params, edit the SUB signature AND every callsite. No "reserved for future use" free passes.
- [Anthropic sandbox + qb64pe bug to report](reference_anthropic_sandbox_qb64pe_bug.md) — Claude Code's Bash sandbox silently blocks writes to `qb64pe/internal/temp/`, failure looks like a parse error. File at github.com/anthropics/claude-code/issues or via `/bug`. Draft body included.
- [Render coords must be LONG](feedback_render_coords_must_be_long.md) — dx/dy/tdx/cv_*/new_cx etc. across SCREEN_render + RENDER_layers/grids/tool_previews + TRANSPARENCY/REFIMG/SYMMETRY/TEXT_render_preview + ZOOM_* SUBs must be LONG. INTEGER overflows on canvasW*zoom for large canvases at high zoom (Z+9/Z+0 on 4480-wide).
- [Screenshot timing for xdotool tests](feedback_screenshot_timing.md) — sleep ≥1.0s after final keyup before `import -window`; PNG file size is a noisy proxy for "did it change" — compare visible content / status bar text, not `ls -l` bytes.
- [pkill -f self-match hangs the shell](feedback_pkill_f_self_match_hang.md) — never `pkill -f`/`pgrep -f <path>` under the agent's eval-wrapped shell; it matches its own argv and SIGTERMs the persistent shell (exit 144, wedged). Kill by exact PID/PGID. GUI binaries: use self-terminating wrapper [DEV/qb64-shot.sh](../../../DEV/qb64-shot.sh) (setsid+timeout+trap+watcher).
- [Ctrl+punctuation needs both detectors](feedback_ctrl_punctuation_cross_platform.md) — Ctrl+, (Settings) etc. must OR `_KEYDOWN(ascii)` (Linux/macOS) with `_KEYHIT` VK code (Windows: comma=188); a `_KEYHIT`-only Windows fix (commit f88b191) silently broke Linux. STATIC edge guard re-armed in outer ELSE.
- [QA harness is the capture path](reference_qa_harness_capture.md) — `QA/draw-qa.sh` drives DRAW via xdotool AND is the only working screenshot path on this Wayland box: ImageMagick `import` has no X11 delegate, `scrot -u` returns black, qb64pe MCP `screenshot` goes through `import` so it fails too. Working recipe = `setsid spectacle -b -n -f -o` + crop.
- [QB64-PE logic operators](qb64pe-logic-operators.md) — `AND`/`OR` do NOT short-circuit (both sides always evaluated → `IF idx >= 1 AND arr(idx)` raises ERR 9 on a 1-based array) and `NOT` is bitwise (`NOT 1` = -2, still truthy). Use `_ANDALSO` / `_ORELSE` / `_NEGATE`, plus `_IIF`. Verified on 4.5.0.
- [CLI ops are console-only ($SCREENHIDE)](draw-cli-console-only-screenhide.md) — app starts `$SCREENHIDE` (no window flash for --help/--version/--options-list/--config-upgrade); `_SCREENSHOW` fires once on the normal-launch path in DRAW.BAS. Consequences: `SCREEN_init` runs while HIDDEN so `_SCREENMOVE`/`_FULLSCREEN` (ERR 5 on hidden window) were moved after `_SCREENSHOW`; startup modals must go through `DRAW_message_box%` (it `_SCREENSHOW`s); `--config-upgrade` is console+exit.
- [config-upgrade MAX_KEYS cap](draw-config-upgrade-maxkeys-cap.md) — `CONFIG_upgrade` re-appended the same trailing keys every run because `MAX_KEYS=200` < real key count (211 + recent files). Bumped to 1024 + overflow `_LOGWARN`. New keys DO get their defaults (CONFIG_set_defaults → CONFIG_save of CFG, not a parse of DRAW.cfg.default). `--config-upgrade` launches the editor after the alert; it doesn't exit.
- [Modal cursor before first render](draw-modal-cursor-before-first-render.md) — in-app modals (message/input box) draw no cursor; they need the OS cursor already showing. One raised before the main loop's first `POINTER_render` (e.g. the `--config-upgrade` alert from `SCREEN_init`) is invisible — `POINTER_init` hid it. Fix lives in `DRAW_message_box%`: `_MOUSESHOW "DEFAULT"` before the loop + re-sync `POINTER.PREV_SYS_CURSOR$` after.
- [QB64-PE reserved words](qb64pe-reserved-words.md) — `pos`, `palette`, `screen`, `color`, `scale`, `step`, `timer` etc. as a variable/param/field name fail with "Name already in use". Prefix instead (`atPos`). Probe with a 6-line file, not a 5-minute full build. (`off` is also reserved — bit the sel-as-shape work.)
- [Effects: selection-as-shape](effects-selection-as-shape.md) — edge/growth effects (shadow, outline, grow, glows, bevel, emboss, chrome) radiate from a marquee selection via `IMGADJ_edge_shape_source&` + `IMGADJ_composite_effect_only` + `IMAGE_ADJ_apply_spatial_edge`; new edge effects must add the same 3-line branch. Color effects still clip. Preview parity via padded loupe capture + `IMGADJ_fx_src&`/`IMAGE_ADJ_fx_preview_finish`.
- [Effect dialog shared widgets](effect-dialog-shared-widgets.md) — reusable pieces from the 2026-08-15 effects overhaul: `DIALOG_dropdown` (4-call immediate-mode popup), `IMGDLG_angle_handle/draw` (slider+dial), `IMGADJ_center_pick_pane` (loupe-pane click-to-center), `MOSAIC_SHAPE_*` cell-ID engine + `IMGADJ_cell_jitter`, `BLEND_ch%`/`BLEND_mix_channels` (all 19 modes), `IMGADJ_dist_transform` (O(pixels) Chebyshev DT), and the `DIALOG_CTX.inputArmed` menu-click-bleed fix. Reuse these before rolling your own. Also: long offscreen QA batches can flake one test (crystallize "app died") — re-run it alone.
- [Effects: Redo/Recall/Blend + menu geometry](effects-last-effect-and-menu-geometry.md) — Last-effect workflow (Ctrl+F / Ctrl+Alt+F / Ctrl+Shift+F, ids **2350-2352**) pinned to EFFECTS-menu top. ⚠️ Two traps: (1) ids 2300-2302 collided with `ANS_ACT_*` export CONSTANTS — the `grep CASE [0-9]` audit can't see literal-vs-const clashes, so also grep the number in `OUTPUT/*.BI`. (2) the menu shift broke QA `open_effect`: recalibrated `top=41` + child-x `490→560`.
- [QB64-PE _MAPUNICODE illegal on some fonts](qb64pe-mapunicode-illegal.md) — `_MAPUNICODE cp TO slot` raises ERR 5 for bitmap/no-cmap font handles (traps per-codepoint, hidden by RESUME NEXT). Render Unicode via `_UPRINTSTRING`(UTF-8) instead; gate expensive token substitutions on `INSTR(template,"{Token}")>0`.
- [QB64-PE shared dynamic UDT array](qb64pe-shared-dynamic-udt-array.md) — share a dynamic array-of-UDT with `REDIM SHARED name(0,0) AS TYPE` in the `.BI`, NOT `DIM SHARED name() AS TYPE` (the latter → `Array 'NAME' (SINGLE) not defined` in reader SUBs under `_EXPLICITARRAY`). Grow inside SUBs with `REDIM [_PRESERVE] ... AS TYPE`.
- [Command palette needs CMD_register](draw-command-palette-registration.md) — a `MENUBAR_register_item` action is NOT in the `?` command palette (and thus not QA-testable via palette) unless also `CMD_register`ed in `CMD_init` (GUI/COMMAND.BM). Missing registration = a QA test that "passes" on a tooltip diff instead of the real dialog.
- [qa-harness toolkit](qa-harness-toolkit.md) — DRAW's QA harness extracted to a standalone cross-app tool at ~/git/qa-harness (core/driver/adapter seam; DRAW + Rust demo both green through bin/qa; no remote yet). draw-qa.sh stays as the reference.
- [Deliver viewables as Artifacts](deliver-viewables-as-artifacts.md) — Rick often runs REMOTE/web Claude Code; local files (images, HTML) can't be reached by his browser. ANYTHING to be viewed → publish as an Artifact (works local + remote), never SendUserFile. Forever.
- [macOS GLUT button emulation](macos-glut-button-emulation.md) — [macOS] Alt+left-click reaches DRAW as button 3 (Ctrl→B2, Option→B3) via Apple-GLUT "Button Emulation" (default ON for trackpad). Breaks Alt+left FG eyedrop; Alt+right works. NOT a DRAW bug — check GLUT Preferences → Mouse first. Probe: DEV/_/test-alt-click.bas.
- [CP_STATE.result clobber gotcha](gotcha-cp-state-result-clobber.md) — never read CP_STATE.result after DRAW_pick_color& returns; the mixer's re-init on close resets it to NONE. Detect cancel by comparing returned color to the initial. Guarded by QA/tests/palette-ops-color-edit.sh.
- [Every fix needs a QA test](every-fix-needs-a-qa-test.md) — Rick's standing rule: every bug fixed gets a QA harness test that guards it (fails on the buggy build, passes on the fixed one). Commit the test with the fix.
- [Effect action-ID collisions](draw-effect-action-id-collisions.md) — new effect/menu IDs must dodge ACTION_SETTINGS=2100 and EXPORT range 2201-2216; CASE ORDER (first match wins) decides the winner, not uniqueness. Collisions false-passed QA (region diff can't tell a real effect from a fullscreen dialog). SHAPE effects live at 2230-2260.
