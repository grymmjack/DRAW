Can you make the image import create the layer first, then render it there in that layer? it's great functionality but i want the stack to be honored.

Also when i create a new layer, it should create the new layer just above the currently selected layer, unless I press SHIFT+Left Click on New Layer button, in which case, top most. Also SHIFT+Left Click on Merge Down can be MERGE ALL (Flatten), and SHIFT+Left Click on up/down layer button arrange can move to top/back accordingly. SHIFT+Left Click on the X should just clear the layer.

IF we drag a layer to the New Layer button it should duplicate it, above the current layer. 

If we drag a layer to the Delete Layer button it should delete it.

The drag to buttons can be single layer options only to simplify things. Doesn't need to support multiple layers for the delete/duplicate when dragged on top of buttons.


