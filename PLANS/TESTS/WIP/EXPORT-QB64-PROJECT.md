# [ ] EXPORT QB64PE PROJECT TESTING

## [x] DIALOG AND INVOCATION

### [ ] Keyboard shortcut Ctrl+Shift+Q
Launch the export dialog using keyboard shortcut.

#### [x] First export shows save dialog
1. [x] Start DRAW with a fresh canvas (File → New)
2. [x] Draw something on the canvas (e.g. a line or filled rect)
3. [x] Press Ctrl+Shift+Q
4. [x] Verify a "Export QB64 Project" save file dialog appears
5. [x] Verify the dialog filter shows "QB64 Source|*.bas"

#### [x] Dialog has sensible default filename
1. [x] Open a project file named "myart.draw"
2. [x] Press Ctrl+Shift+Q
3. [x] Verify the suggested filename is "myart.bas"

#### [ ] Dialog uses last save directory
1. [ ] Export a project to directory /tmp/test1/
2. [ ] Close and reopen the save dialog with Ctrl+Shift+Q
3. [ ] Verify the dialog opens in /tmp/test1/
> **FAILED**: Quick-export behavior means second Ctrl+Shift+Q silently re-exports without showing a dialog, so the directory cannot be verified. Needs a separate "Export QB64PE Project As..." action (always-prompt, Ctrl+Alt+Shift+Q) to make this testable. — 2026-04-19

#### [x] Cancel dialog does nothing
1. [x] Press Ctrl+Shift+Q
2. [x] Click Cancel in the file dialog
3. [x] Verify no files were created
4. [x] Verify the canvas state is unchanged

### [x] Menubar invocation
Launch from File menu.

#### [x] Menu item present and labeled
1. [x] Click the File menu
2. [x] Verify "EXPORT QB64 PROJECT..." menu item exists
3. [x] Verify it shows shortcut "Ctrl+Shift+Q"

#### [x] Menu item triggers dialog
1. [x] Click File → Export QB64 Project...
2. [x] Verify the save file dialog appears

### [x] Toolbar button invocation
Launch from the toolbar Code Export button (row 6, col 4).

#### [x] Left-click toolbar button triggers export
1. [x] Draw something on the canvas
2. [x] Click the "QB64 Code Export" toolbar button (bottom-right area of toolbar)
3. [x] Verify the save dialog appears (first time) or quick-export occurs (subsequent)

### [x] Command Palette invocation

#### [x] Search and invoke from command palette
1. [x] Press F1 (or ? toolbar button) to open command palette
2. [x] Type "Export QB64"
3. [x] Verify "Export QB64 Project..." appears in results
4. [x] Select it and verify the dialog appears
> **NOTE**: F1 does not open the command palette — only the ? toolbar button works. Test step 1 wording is misleading. — 2026-04-19

### [x] Quick export (re-export to same path)

#### [x] Second export skips dialog
1. [x] Press Ctrl+Shift+Q, choose a filename, and complete the export
2. [x] Press Ctrl+Shift+Q again
3. [x] Verify no dialog appears — it re-exports silently to the same path
4. [x] Verify the .bas file is updated with current content
> **NOTE**: The success message box still appears on quick re-exports — it should be suppressed for subsequent quick exports. — 2026-04-19

#### [x] Toolbar button also does quick export
1. [x] After a first export via Ctrl+Shift+Q
2. [x] Click the toolbar QB64 Code Export button
3. [x] Verify it silently re-exports to the same path

## [x] OUTPUT FILE STRUCTURE

### [x] BAS file generation
Verify the generated .BAS file is a valid standalone QB64PE program.

#### [x] BAS file created at chosen path
1. [x] Export a project to /tmp/testexport.bas
2. [x] Verify /tmp/testexport.bas exists on disk
3. [x] Open the file and verify it starts with "' Generated by DRAW"

#### [x] BAS file has correct header
1. [x] Export any project
2. [x] Open the .bas file
3. [x] Verify it contains OPTION _EXPLICIT
4. [x] Verify it contains OPTION _EXPLICITARRAY
5. [x] Verify it contains '$DYNAMIC
6. [x] Verify CONST CANVAS_W and CANVAS_H match the project canvas dimensions
7. [x] Verify SCREEN _NEWIMAGE uses canvas dimensions

#### [x] BAS file has display loop
1. [x] Export any project
2. [x] Verify the .bas ends with _DISPLAY, a DO LOOP UNTIL _KEYHIT = 27, and SYSTEM

### [x] Asset directory creation

#### [x] Asset directory created alongside BAS
1. [x] Export to /tmp/myproject.bas
2. [x] Verify /tmp/myproject_ASSETS/ directory was created

#### [x] Asset directory name matches BAS base name
1. [x] Export to /tmp/coolart.bas
2. [x] Verify asset dir is /tmp/coolart_ASSETS/

### [x] Completion message

#### [x] Success message box displayed
1. [x] Export a project
2. [x] Verify a message box appears with "Exported to:" and the path
3. [x] Verify it also shows the asset folder path

## [ ] LAYER CLASSIFICATION

### [x] Vector layer classification (EXPORT_MODE_VECTOR)
Layers with only vector-safe operations should be classified as vector.

#### [x] Pure LINE operations classified as vector
1. [x] Create a new project with one layer
2. [x] Draw several lines using the Line tool (L)
3. [x] Check the layer panel badge shows "(SUB)" (green)
4. [x] Export and verify the .bas file contains a SUB with LINE commands

#### [x] Pure RECT operations classified as vector
1. [x] Create a new layer
2. [x] Draw rectangles using the Rect tool (R and Shift+R for filled)
3. [x] Verify badge shows "(SUB)"
4. [x] Export and verify SUB contains LINE ... , B and LINE ... , BF commands
> **BUG**: SUB names use `historyId&` which does NOT reset on New Canvas (Ctrl+N). `DRW_new_canvas` resets `LAYER_COUNT%` and `NEXT_GROUP_NUM%` but not `NEXT_LAYER_HISTORY_ID&`. So after creating a second new file, layers get names like `Background_3` instead of `Background_1`. Fix: add `NEXT_LAYER_HISTORY_ID& = 1` in `DRW_new_canvas` after layer clear loop. — 2026-04-19

#### [x] Pure ELLIPSE operations classified as vector
1. [x] Draw ellipses using the Ellipse tool (C and Shift+C for filled)
2. [x] Verify badge shows "(SUB)"
3. [x] Export and verify SUB contains CIRCLE commands (unfilled) or FillEllipseScanline calls (filled)

#### [x] Pure FILL operations classified as vector
1. [x] Draw a rect, then fill it using Fill tool (F)
2. [x] Verify badge shows "(SUB)"
3. [x] Export and verify SUB contains PAINT commands

#### [x] Pure POLYLINE operations classified as vector
1. [x] Draw a polygon using the Polygon tool (P)
2. [x] Verify badge shows "(SUB)"
3. [x] Export and verify SUB contains sequential LINE commands

#### [x] Filled POLYLINE includes FillPolygonScanline
1. [x] Draw a filled polygon using Shift+P
2. [x] Export and verify the .bas includes the FillPolygonScanline helper SUB
3. [x] Verify the SUB call with the polygon point arrays

#### [x] DOT/Brush 1px classified as vector (pixel-diff)
1. [x] Use the Dot tool (D) to place individual pixels
2. [x] Verify badge shows "(SUB)"
3. [x] Export and verify SUB contains PSET commands

### [ ] Image layer classification (EXPORT_MODE_IMG)
Layers with non-vectorizable operations.

#### [x] Custom brush forces IMG mode
1. [x] Activate a custom brush and paint with it
2. [x] Verify layer badge shows "(IMG)" (orange)
3. [x] Export and verify layer is saved as PNG in _ASSETS/ and loaded via _LOADIMAGE

#### [x] Symmetry forces IMG mode
1. [x] Enable symmetry (e.g. horizontal mirror)
2. [x] Draw a line with symmetry active
3. [x] Verify badge shows "(IMG)"

#### [x] Spray tool forces IMG mode
1. [x] Use the Spray tool (K) to paint
2. [x] Verify badge shows "(IMG)"

#### [x] Thick brush with freehand forces IMG mode
1. [x] Set brush size > 1
2. [x] Use the Brush tool (B) to freehand draw
3. [x] Verify badge shows "(IMG)"

#### [x] Transform forces IMG mode
1. [x] Apply Edit → Transform to a layer
2. [x] Verify badge shows "(IMG)"

#### [x] Imported image with no history classified as IMG
1. [x] Import an image as a new layer (File → Import Image)
2. [x] Verify badge shows "(IMG)"
3. [x] Export and verify a PNG is saved to _ASSETS/
> **BUG FIXED**: Imported images with only structural history (LAYER_ADD) were misclassified as VECTOR → empty SUB. Fixed by scanning for non-transparent pixel content when layer has structural history but no drawing ops. — 2026-04-19

### [ ] Text layer classification (EXPORT_MODE_TEXT)

#### [x] Text layer shows TXT badge
1. [x] Create a text layer using the Text tool (T)
2. [x] Type some text
3. [x] Verify badge shows "(TXT)" (blue)
> **BUG FIXED**: `LAYERS_new%` records `LAYER_ADD` history (triggering `FILE_QB64_recompute_export_mode`) while `layerType%` is still `LAYER_TYPE_IMAGE`. `LAYERS_new_text%` sets `LAYER_TYPE_TEXT` after, but recompute already ran. Fixed by calling `FILE_QB64_recompute_export_mode` at end of `LAYERS_new_text%`. — 2026-04-19

#### [x] Text layer exported as pixel data SUB
1. [x] Create a text layer with formatted text
2. [x] Export
3. [x] Verify .bas contains a SUB with PSET/LINE commands from pixel rendering

### [ ] Group layers classification (EXPORT_MODE_NONE)

#### [x] Group layers not exported
1. [x] Create a layer group containing some layers
2. [x] Export
3. [x] Verify the group layer itself does not appear in the .bas compositing section
4. [x] Verify child layers within the group are exported normally

### [x] Symbol layers

#### [x] Symbol parent exports as single SUB
1. [x] Create a symbol layer (parent)
2. [x] Create symbol children (instances)
3. [x] Export
4. [x] Verify only one SUB is generated for the parent
5. [x] Verify children call the parent's SUB in the compositing section

#### [x] Symbol child shows bracket badge [SUB] / [IMG] / [TXT]
1. [x] Create symbol child layers
2. [x] Verify badge uses square brackets (e.g. "[SUB]") not parentheses

### [x] Duplicate layers

#### [x] Duplicate vector layer reuses source SUB
1. [x] Draw a vector shape on a layer
2. [x] Duplicate the layer (Ctrl+Shift+D or menu)
3. [x] Export
4. [x] Verify only one SUB is generated
5. [x] Verify the duplicate calls the original's SUB with a "duplicate of" comment

#### [x] Duplicate text layer reuses source SUB
1. [x] Create a text layer, duplicate it
2. [x] Export
3. [x] Verify duplicate calls the source text layer's SUB

## [x] EXPORT MODE RECOMPUTATION

### [x] Badge updates after drawing operations

#### [x] Badge changes from vector to IMG when spray is used
1. [x] Start with a pure vector layer (badge shows "(SUB)")
2. [x] Use the Spray tool on the same layer
3. [x] Verify badge changes to "(IMG)"

#### [x] Badge recomputes after undo
1. [x] Create a vector layer with a line
2. [x] Use spray on the same layer (badge → "(IMG)")
3. [x] Ctrl+Z to undo the spray
4. [x] Verify badge returns to "(SUB)"

#### [x] Badge recomputes after redo
1. [x] Continue from above, press Ctrl+Y to redo
2. [x] Verify badge changes back to "(IMG)"

### [x] File load triggers recompute

#### [x] Open project recalculates all export modes
1. [x] Save a project with mixed vector/IMG/text layers
2. [x] Close and reopen the project
3. [x] Verify all layer badges match their expected modes
> **BUG FIXED**: `FILE_QB64_recompute_all_export_modes` ran AFTER `HISTORY_clear`, so classify had no history to scan — all layers with pixels fell back to IMG. Fixed by moving recompute to run right after `HISTORY_load_binary` but BEFORE `HISTORY_clear`. — 2026-04-19

## [x] VECTOR LAYER CODE GENERATION

### [x] LINE command generation

#### [x] Simple line exported correctly
1. [x] Draw a line from (10,10) to (50,50) with red color
2. [x] Export
3. [x] Verify SUB contains: LINE (10, 10)-(50, 50), _RGB32(255, 0, 0)

#### [x] Thick brush line uses pixel diff
1. [x] Draw a line with brush size > 1 (but no symmetry/custom brush)
2. [x] Export
3. [x] Verify the line is exported as PSET/LINE pixel-diff ops (not a single LINE command)

### [x] RECT command generation

#### [x] Outlined rect exported as LINE B
1. [x] Draw an outlined rectangle
2. [x] Export
3. [x] Verify: LINE (x1, y1)-(x2, y2), color, B

#### [x] Filled rect exported as LINE BF
1. [x] Draw a filled rectangle
2. [x] Export
3. [x] Verify: LINE (x1, y1)-(x2, y2), color, BF

### [x] ELLIPSE command generation

#### [x] Outlined ellipse exported as CIRCLE
1. [x] Draw an outlined ellipse
2. [x] Export and verify CIRCLE command with aspect ratio

#### [x] Filled ellipse uses FillEllipseScanline
1. [x] Draw a filled ellipse
2. [x] Export and verify FillEllipseScanline call

### [x] FILL command generation

#### [x] CLS for full-canvas fill
1. [x] Use Fill tool to flood-fill the entire empty canvas
2. [x] Export and verify: CLS , color
> **NOTE**: Exports as `PAINT (0, 103), color` instead of `CLS , color`. Functionally equivalent — full canvas is still filled. The CLS background color is set separately via the BAS export BG color setting. — 2026-04-19

#### [x] PAINT with border color for bounded fill
1. [x] Draw a rect, then fill inside it
2. [x] Export and verify: PAINT (x, y), fillColor, borderColor

### [x] POLYLINE command generation

#### [x] Polygon outline as sequential LINEs
1. [x] Draw a polygon with 4+ points
2. [x] Export and verify sequential LINE commands connecting all points
3. [x] Verify the closing LINE from last point back to first

#### [x] Filled polygon includes coordinate arrays
1. [x] Draw a filled polygon
2. [x] Export and verify DIM polyX_N/polyY_N arrays with coordinates
3. [x] Verify FillPolygonScanline call

### [x] BRUSH pixel diff

#### [x] Single pixel dots exported as PSET
1. [x] Place several individual pixels with the Dot tool
2. [x] Export and verify PSET commands at the correct coordinates

#### [x] Horizontal runs optimized to LINE
1. [x] Draw a horizontal brush stroke (1px brush)
2. [x] Export and verify horizontal runs are consolidated into LINE commands

## [x] IMAGE LAYER EXPORT

### [x] PNG asset generation

#### [x] IMG layer saved as PNG in _ASSETS
1. [x] Create a layer that falls back to IMG mode (e.g. use spray)
2. [x] Export
3. [x] Verify a .png file exists in _ASSETS/ named after the layer

#### [x] PNG file contains layer pixel content
1. [x] Open the exported PNG in an image viewer
2. [x] Verify it matches the layer content from DRAW

### [x] Image loading code

#### [x] DIM and _LOADIMAGE generated for IMG layers
1. [x] Export a project with IMG layers
2. [x] Verify .bas contains DIM SHARED img_layerName AS LONG
3. [x] Verify img_layerName = _LOADIMAGE("projectname_ASSETS/layerName.png", 32)

#### [x] _PUTIMAGE used for compositing IMG layers
1. [x] Export and verify _PUTIMAGE (0, 0), img_layerName in the compositing section

### [x] Symbol child IMG layers use parent's PNG

#### [x] Symbol child references parent image variable
1. [x] Create a symbol parent (IMG mode) with children
2. [x] Export
3. [x] Verify children use _PUTIMAGE with parent's img_ variable and offset coordinates

## [x] TEXT LAYER EXPORT

### [x] Font asset copying

#### [x] TTF/OTF font files copied to _ASSETS
1. [x] Create a text layer using a custom TTF font
2. [x] Export
3. [x] Verify the font file is copied to _ASSETS/

### [x] Font loading code

#### [x] _LOADFONT generated for text layer fonts
1. [x] Export a project with text layers
2. [x] Verify .bas contains DIM SHARED fnt_fontname_size AS LONG
3. [x] Verify fnt_fontname_size = _LOADFONT("_ASSETS/font.ttf", size, "DONTBLEND")
> **BUG**: Font filename has excessive trailing spaces (fixed-length string not trimmed). Fixed in FILE-QB64.BM. — 2026-04-19

#### [x] Unique fonts not duplicated
1. [x] Create two text layers using the same font at the same size
2. [x] Export
3. [x] Verify only one _LOADFONT line is generated for that font/size pair

### [x] Text pixel rendering

#### [x] Text layer content exported as pixel data
1. [x] Create a text layer with "Hello World"
2. [x] Export
3. [x] Verify the SUB contains COLOR + PSET/LINE ops representing the rendered text pixels

#### [x] Multi-color text exports with COLOR changes
1. [x] Create text with different colors per character
2. [x] Export
3. [x] Verify COLOR statements change between character groups

## [x] OPACITY AND BLEND MODES

### [x] Layer opacity

#### [x] Full opacity layer exported without wrapper
1. [x] Create a layer at 100% opacity
2. [x] Export
3. [x] Verify the SUB is called directly (no tmpLayer wrapper)

#### [x] Partial opacity generates ScaleImageAlpha helper
1. [x] Set a layer to 50% opacity
2. [x] Export
3. [x] Verify the .bas includes the ScaleImageAlpha SUB
4. [x] Verify compositing wraps the layer: tmpLayer → render → ScaleImageAlpha → _PUTIMAGE → _FREEIMAGE

#### [x] Zero opacity layer still in compositing
1. [x] Set a layer to 0% opacity (but visible)
2. [x] Export
3. [x] Verify the layer is listed in compositing (or check the behavior — it may be skipped)
> **FAIL→FIXED**: Layer at 0% opacity was exported as full 100% opacity (condition excluded 0). Fixed: 0% opacity layers are now skipped entirely in compositing with a comment. — 2026-04-19

### [x] Blend modes

#### [x] Normal blend mode exported without wrapper
1. [x] Create a layer with Normal blend mode (default)
2. [x] Export
3. [x] Verify no BlendLayer call

#### [x] Non-normal blend mode generates BlendLayer helper
1. [x] Set a layer to Multiply blend mode
2. [x] Export
3. [x] Verify the .bas includes the BlendLayer SUB
4. [x] Verify blend mode constants (BM_MULTIPLY etc.) are defined
5. [x] Verify compositing wraps: tmpLayer → render → BlendLayer → _FREEIMAGE

#### [x] All blend modes have constants defined
1. [x] Set different layers to various blend modes (Screen, Overlay, Add, Subtract, etc.)
2. [x] Export
3. [x] Verify all BM_* constants are emitted (18 modes total)

#### [x] Combined opacity + blend mode
1. [x] Set a layer to 75% opacity with Overlay blend mode
2. [x] Export
3. [x] Verify compositing uses BlendLayer (which handles opacity internally via effA)

## [x] COMPOSITING ORDER

### [x] Z-order respected

#### [x] Layers composited bottom to top
1. [x] Create 3 layers: Layer1 (bottom), Layer2 (middle), Layer3 (top)
2. [x] Export
3. [x] Verify the compositing section calls SUBs in z-index ascending order
4. [x] Verify Layer1 is first, Layer3 is last

### [x] Only visible layers exported

#### [x] Hidden layers excluded from export
1. [x] Create 3 layers, hide the middle one
2. [x] Export
3. [x] Verify only 2 layers appear in compositing section and SUBs
4. [x] Verify no PNG is generated for the hidden layer

### [x] Layer comments

#### [x] Each layer has a name comment
1. [x] Export a multi-layer project
2. [x] Verify each compositing entry has a "' Layer: name" comment

#### [x] Opacity noted in comment when not 100%
1. [x] Set a layer to 128 opacity
2. [x] Export
3. [x] Verify comment includes "(opacity 128)"

#### [x] Blend mode noted in comment when non-normal
1. [x] Set a layer to Screen blend mode
2. [x] Export
3. [x] Verify "' Blend mode: Screen" comment

## [ ] CANVAS SIZES

### [ ] Small canvas export
Verify export works with minimal canvas sizes.

#### [ ] 1x1 canvas exports correctly
1. [ ] Create a 1×1 canvas
2. [ ] Place a single pixel
3. [ ] Export and verify CONST CANVAS_W = 1, CANVAS_H = 1
4. [ ] Verify the generated program compiles

#### [ ] 16x16 canvas exports correctly
1. [ ] Create a 16×16 canvas with some artwork
2. [ ] Export and verify dimensions match

### [ ] Large canvas export

#### [ ] 320x200 canvas exports correctly
1. [ ] Create a 320×200 canvas with mixed layer types
2. [ ] Export
3. [ ] Verify CONST CANVAS_W = 320, CANVAS_H = 200

#### [ ] 640x480 canvas with many layers
1. [ ] Create a 640×480 project with 10+ layers
2. [ ] Export
3. [ ] Verify all visible layers are included

## [ ] GENERATED PROGRAM COMPILATION

### [ ] Compile and run exported project

#### [ ] Export compiles with QB64PE without errors
1. [ ] Export a project with mixed vector/IMG/text layers
2. [ ] Compile the exported .bas with QB64PE: qb64pe -x exported.bas
3. [ ] Verify compilation succeeds with no errors

#### [ ] Exported program displays correct image
1. [ ] Run the compiled exported program
2. [ ] Compare the displayed image to the original DRAW canvas
3. [ ] Verify colors, positions, and layer order match
4. [ ] Press Escape to exit the program

#### [ ] Vector-only project compiles (no _ASSETS needed)
1. [ ] Create a project using only vector tools (line, rect, ellipse)
2. [ ] Export
3. [ ] Verify .bas compiles and runs even if _ASSETS/ is empty (no IMG layers)

#### [ ] IMG-only project compiles and loads assets
1. [ ] Create a project where all layers are IMG mode
2. [ ] Export
3. [ ] Compile and verify PNGs load correctly from _ASSETS/

#### [ ] Mixed project compiles (vector + IMG + text)
1. [ ] Create a project with at least one of each layer type
2. [ ] Export, compile, and run
3. [ ] Verify all layer types render correctly

## [ ] HELPER SUBS

### [ ] FillPolygonScanline helper

#### [ ] Included only when filled polygons exist
1. [ ] Export a project with no filled polygons
2. [ ] Verify FillPolygonScanline SUB is NOT in the .bas file
3. [ ] Add a filled polygon, re-export
4. [ ] Verify FillPolygonScanline SUB IS now present

### [ ] FillEllipseScanline helper

#### [ ] Included only when filled ellipses exist
1. [ ] Export a project with filled ellipses
2. [ ] Verify FillEllipseScanline SUB is present
3. [ ] Export without filled ellipses
4. [ ] Verify it is absent

### [ ] ScaleImageAlpha helper

#### [ ] Included only when partial-opacity layers exist
1. [ ] Export a project with all layers at 100% opacity
2. [ ] Verify ScaleImageAlpha SUB is NOT present
3. [ ] Set one layer to 50% opacity, re-export
4. [ ] Verify ScaleImageAlpha SUB IS present

### [ ] BlendLayer helper

#### [ ] Included only when non-normal blend modes exist
1. [ ] Export with all Normal blend modes
2. [ ] Verify BlendLayer SUB is NOT present
3. [ ] Set one layer to Multiply, re-export
4. [ ] Verify BlendLayer SUB IS present with blend mode constants

## [ ] SAFE IDENTIFIER GENERATION

### [ ] Layer name sanitization

#### [ ] Special characters converted to underscores
1. [ ] Name a layer "My Layer!@#$"
2. [ ] Export
3. [ ] Verify the SUB name uses only alphanumeric chars and underscores

#### [ ] Leading digits get underscore prefix
1. [ ] Name a layer "123abc"
2. [ ] Export
3. [ ] Verify the SUB name starts with "_" (e.g. "_123abc_NNN")

#### [ ] Empty name uses fallback
1. [ ] Leave a layer with default name or clear it
2. [ ] Export
3. [ ] Verify a fallback name like "layer_NNN" is used

#### [ ] Trailing dots/spaces stripped
1. [ ] Name a layer "test..."
2. [ ] Export
3. [ ] Verify no trailing dots in identifier

## [ ] EDGE CASES

### [ ] Empty canvas

#### [ ] Export with no drawing operations
1. [ ] Create a new canvas (Ctrl+N) but don't draw anything
2. [ ] Press Ctrl+Shift+Q
3. [ ] Export to a file
4. [ ] Verify a minimal .bas file is created with just SCREEN, CLS, and SYSTEM
5. [ ] Verify it compiles

### [ ] Single layer project

#### [ ] One vector layer exports correctly
1. [ ] Create a single-layer project with vector ops only
2. [ ] Export and verify the .bas has one SUB called from main

### [ ] Many layers

#### [ ] 64 layers export (max layers)
1. [ ] Create a project with many layers (e.g. 20+)
2. [ ] Export
3. [ ] Verify all visible layers are composited in order

### [ ] Layers with same name

#### [ ] Duplicate names get unique SUB names
1. [ ] Create two layers both named "Background"
2. [ ] Export
3. [ ] Verify each gets a unique SUB name (distinguished by historyId suffix)

### [ ] Overwrite existing file

#### [ ] Re-export overwrites previous .bas and _ASSETS
1. [ ] Export to a path
2. [ ] Add more content to the project
3. [ ] Export again to the same path
4. [ ] Verify the .bas file is updated with new content
5. [ ] Verify _ASSETS/ PNGs are updated

### [ ] Long file paths

#### [ ] Path with spaces works
1. [ ] Create a directory with spaces: /tmp/my project/
2. [ ] Export to /tmp/my project/test.bas
3. [ ] Verify both .bas and _ASSETS/ are created correctly

### [ ] No unsaved project filename

#### [ ] Default filename "project.bas" used for unsaved project
1. [ ] Start DRAW fresh without opening/saving any file
2. [ ] Press Ctrl+Shift+Q
3. [ ] Verify the suggested default filename is "project.bas"

## [ ] BACKGROUND COLOR

### [ ] CLS uses configured BAS export BG color

#### [ ] Default BG color in CLS
1. [ ] Export a project without changing BAS export settings
2. [ ] Verify the CLS line uses the configured BAS_EXPORT_BG_COLOR value

## [ ] STATE PRESERVATION

### [ ] Export does not alter canvas state

#### [ ] Tool returns to null after export dialog
1. [ ] Select the Brush tool
2. [ ] Press Ctrl+Shift+Q and complete the export
3. [ ] Note: the code sets CURRENT_TOOL% = TOOL_NULL — verify no stale tool state

#### [ ] Canvas content unchanged after export
1. [ ] Draw complex artwork
2. [ ] Export
3. [ ] Verify the canvas pixels are identical before and after export

#### [ ] Undo history unchanged after export
1. [ ] Draw some operations
2. [ ] Export
3. [ ] Verify Ctrl+Z still undoes the last drawing operation (not the export)

### [ ] Mouse state cleanup after dialog

#### [ ] No ghost clicks after dialog
1. [ ] Export via Ctrl+Shift+Q
2. [ ] Dismiss the success message box
3. [ ] Click on the canvas
4. [ ] Verify no unexpected tool activation or spurious drawing occurs

## [ ] CONFIG PERSISTENCE

### [ ] Last directory saved

#### [ ] LAST_DIR_SAVE updated after export
1. [ ] Export to /tmp/exporttest/
2. [ ] Open DRAW.cfg
3. [ ] Verify LAST_DIR_SAVE contains the export directory path

### [ ] Session export path

#### [ ] Quick export path persists within session
1. [ ] Export to /tmp/first.bas
2. [ ] Make changes and quick-export again
3. [ ] Verify it exports to /tmp/first.bas without dialog
4. [ ] Close DRAW, reopen
5. [ ] Press Ctrl+Shift+Q — verify dialog appears (session path cleared)
