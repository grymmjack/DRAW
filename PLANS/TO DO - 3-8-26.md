# TO DO 



## PATTERN TILE MODE
- View:
  - --- DIVIDER ---
  - [x] PATTERN TILE MODE (<= 512px) ... SHIFT+TAB
    - Only available if canvas size is <= 512 x 512
    - If canvas is > 512px in any dimension - show as grayed out/disabled
    - Checkable option - OFF by default
    - Checked state IS SAVED with file for later edits

### PATTERN TILE MODE FUNCTION
- ON: 
  - Canvas content repeats one time in every direction and diagonals: 

    ```
    - Where [C] is cloned copy of [S]OURCE 
    - [C] regions are not part of the actual canvas in the file, etc.
    - They are only shown and active when View -> PATTERN TILE MODE is active.
    - The are rendered and laid out in a 3x3 grid to assist in the creation of patterns and tiles.

    3x3 grid:

    C C C
    C S C
    C C C

    S = SOURCE canvas (actually part of the file when saved, etc.)
    C = CLONED canvas
    ```

  - All regions and clones are editable and apply edits to the [S]
  - When [S] changes, update in real time [C]
  - Show cursors, assistants, etc. in [C] just as you do [S] but:
    - Do not have duplicate cursors or assistants.
    - For example if the user is painting on a [C], show the same cursors/UX
      as when they are painting on [S], allow same functions.
    - If the user is using color picker on [C], show same cursor/UX, etc.
  - SELECTIONS should not be rendered more than once. 

- OFF:
  - Normal mode as it is now. 



## GRADIENT MODE REFINEMENT
- Make all bins the same size - whatever Brush bins size is make patterns, gradients, all bins same size
- Gradient colors should not be spread across the entire canvas
- The gradients are currently acting like a wallpaper, where regardless of tool, it's 
  always showing the same thing. It's acting more like a mask that tools reveal the gradient
  from. That's not how it should work.
- Gradient colors should stretch/fit inside / spread across the drawing tool boundaries
  - When drawing with a line, from the start of the line to the end of the line
  - When drawing with a brush, from the start of the brush stroke to when user lifts it
  - When using a fill, should fill with gradient at the boundaries of the fill
  - When using a shape, should fill with gradient at the boundaries of the shape
  - Rect, Poly line, Ellipse - all should just use the gradient colors to plot them 
    - As the stroke/at the extents and flow around the shape
  - Filled Rect, Filled Poly line, Filled Ellipse - all should fill like fill tool
    but only within the shape of the shape, the boundaries, etc.
  - When filling selections should fill to the extents of the actual selection
- Every time the user draws something with a gradient it should determine:
  - Where on X to start the gradient from
  - Where on Y to start the gradient from
  - Where on X to end the gradient at
  - Where on Y to end the gradient at
  - Including filled areas with scanline algorithm, etc for shapes that are not simple.
- The gradient editor should provide a dialog that looks like the image
  - PLANS/GRADIENT-EDITOR-DIALOG.png
  - This editor dialog should be entirely themeable
    - Following suit and infering what should be done and how things are named
  - Use this URL to understand how gradient editing should work
    - #fetch https://www.teachucomp.com/how-to-use-the-gradient-editor-in-photoshop-instructions/






## BINS CLICK FIX
x Make clicking on background in between controls do nothing
x Right now it's drawing when the cursor is between controls


## COLOR PICKER LOUPE
x Add 1px STROKE COLOR (put in DRAW.cfg) around TEXT for RGB and hex colors
x DO NOT show color picker when hovering over any UX like menu, toolbox, layer panel, bins, drawer


## BINS (common for all bins)
x Add right click context menu
  x SELECT - Left Click (show as disabled for hint)
  x STORE - SHIFT+Left Click (show as disabled for hint)
  x CLEAR - SHIFT+Middle CLick (show as disabled for hint)
  x LOAD - SHIFT+Right Click (show as disabled for hint)
  x --- DIVIDER ---
  x-Load Drawer Set (.dset)
  x Save Drawer Set (.dset)
  x- Clear Drawer Set
  x --- DIVIDER ---
  x Explore Folder
x Modify input handling to suit the above if it's different.

### EXTRA context menu options for PATTERNS (at the top)
x SOURCE = CLIPBOARD (show as disabled for hint)
x --- DIVIDER ---

### EXTRA context menu options for GRADIENTS (at the top)
x EDIT = Double Click (show as disabled for hint)

### EXTRA context menu options for BRUSHES (at the top)
x SOURCE = CLIPBOARD or CUSTOM BRUSH (show as disabled for hint)


## DRAWING MODES IN MENU
x Place at the top of the TOOLS menu:
x Tools:
  x DRAWING MODE: COLOR - F1
  x DRAWING MODE: GRADIENTS - F2
  x DRAWING MODE: PATTERNS- F3
  x --- DIVIDER ---
  x ... remaining tool options ....


## PREVIEW WINDOW
x A resizable preview window:
  x That sits on top of UI
  x Preview window can be resized and moved around
  x Preview window state is remembered across sessions:
  x [x] Follow pointer as a universal magnifier
    x X, Y position
    x W, H of preview window
    x Pan position of preview
    x Zoom level of preview
    x Open/closed state of preview window    
  x Buttons in window title bar:
    x X CLOSE
    x _ MINIMIZE
  x Under preview window:
    x [x] Follow pointer
      - Automatically pans to have center of preview window directly where
        mouse X,Y pointer is when enabled
  x Can be panned / zoomed independently
    x Pan: click and drag on preview image
      x When mouse over preview window, show hand cursor to indicate pan
    x Zoom: when over preview image wheel
  x When resizing window, use arrow pointers and corner pointers
x Menu addition: 
  x Position directly under View -> Color Strip/Status
  x View:
    x Preview Window [x] (F4 hotkey)
x Preview Window should auto hide when dragged over while drawing and redisplay when done drawing
x Preview Window should be included in View: Hide/Show All as part of "All"


## EDIT BAR
x Bar that contains quick icons to click from the EDIT menu (except transform submenu)
x Bar can be configured for placement: 
  x LEFT adjacent to the LAYERS PANEL
  x RIGHT adjacent to the TOOLBOX / DRAWER / BINS
x DRAW.cfg entry for:
  x EDIT_BAR_DOCK_POSITION
    x LEFT
    x RIGHT
x Bar icons are layed out in same order as EDIT menu (omit transform and transform submenu items)
x Where there is a menu divider, add vertical space to create margin/space between icons in bar
  x USE EDIT_BAR_DIVIDER_VERT_MARGIN for this amount of space
x When a icon is hovered over, or clicked on
  x Show a square rect outline around it in color: EDIT_BAR_ICON_MOUSE_OUTLINE_COLOR
    x 16x16 rect (or extents of the image boundaries for the icon)
x When an icon is clicked, it should engage the program the same way as if clicking the edit menu bar item
x Edit Bar should auto hide when dragged over while drawing and redisplay when done drawing
x Edit Bar should be included in View: Hide/Show All as part of "All"
x Menu addition: 
  x Position directly under View -> Color Strip/Status
  x View:
    x EDIT BAR [x] (F5 hotkey)
x Images for Edit Bar icons are in THEME/IMAGES/EDITBAR
x THEME.cfg entries for:
  x EDIT_BAR_LEFT_BORDER_COLOR
  x EDIT_BAR_RIGHT_BORDER_COLOR
  x EDIT_BAR_LEFT_BORDER_WIDTH
  x EDIT_BAR_RIGHT_BORDER_WIDTH
  x EDIT_BAR_WIDTH
  x EDIT_BAR_ICON_PADDING (default to 0)
  x EDIT_BAR_BG_COLOR
    x SPAN entire height of the program window from start position at bottom of menu
      x to bottom of program window
  x EDIT_BAR_ICON_MOUSE_OUTLINE_COLOR
  x EDIT_BAR_DIVIDER_VERT_MARGIN (this is vertical space between groups)
  x EDIT_BAR_ICON_UNDO
  x EDIT_BAR_ICON_REDO
  x EDIT_BAR_ICON_CUT
  x EDIT_BAR_ICON_COPY
  x EDIT_BAR_ICON_COPY_MERGED
  x EDIT_BAR_ICON_PASTE
  x EDIT_BAR_ICON_PASTE_IN_PLACE
  x EDIT_BAR_ICON_CUT_TO_LAYER
  x EDIT_BAR_ICON_COPY_TO_LAYER
  x EDIT_BAR_ICON_CLEAR
  x EDIT_BAR_ICON_FILL_WITH_FG
  x EDIT_BAR_ICON_FILL_WITH_BG
  x EDIT_BAR_ICON_STROKE_SELECTION
  x EDIT_BAR_ICON_FLIP_HORIZONTAL
  x EDIT_BAR_ICON_FLIP_VERTICAL
  x EDIT_BAR_ICON_SCALE_DOWN
  x EDIT_BAR_ICON_SCALE_UP
  x EDIT_BAR_ICON_ROTATE_CW
  x EDIT_BAR_ICON_ROTATE_CCW
