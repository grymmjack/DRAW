# TO DO 



## GRADIENT MODE REFINEMENT
x Make all bins the same size x whatever Brush bins size is make patterns, gradients, all bins same size
x Gradient colors should not be spread across the entire canvas
x The gradients are currently acting like a wallpaper, where regardless of tool, it's 
  always showing the same thing. It's acting more like a mask that tools reveal the gradient
  from. That's not how it should work.
x Gradient colors should stretch/fit inside / spread across the drawing tool boundaries
  x When drawing with a line, from the start of the line to the end of the line
  x When drawing with a brush, from the start of the brush stroke to when user lifts it
  x When using a fill, should fill with gradient at the boundaries of the fill
  x When using a shape, should fill with gradient at the boundaries of the shape
  x Rect, Poly line, Ellipse x all should just use the gradient colors to plot them 
    x As the stroke/at the extents and flow around the shape
  x Filled Rect, Filled Poly line, Filled Ellipse x all should fill like fill tool
    but only within the shape of the shape, the boundaries, etc.
  x When filling selections should fill to the extents of the actual selection
x Every time the user draws something with a gradient it should determine:
  x Where on X to start the gradient from
  x Where on Y to start the gradient from
  x Where on X to end the gradient at
  x Where on Y to end the gradient at
  x Including filled areas with scanline algorithm, etc for shapes that are not simple.
x The gradient editor should provide a dialog that looks like the image
  x PLANS/GRADIENT-EDITOR-DIALOG.png
  x This editor dialog should be entirely themeable
    x Following suit and infering what should be done and how things are named
  x Use this URL to understand how gradient editing should work
    x #fetch https://www.teachucomp.com/how-to-use-the-gradient-editor-in-photoshop-instructions/






## BINS CLICK FIX
x Make clicking on background in between controls do nothing
x Right now it's drawing when the cursor is between controls


## COLOR PICKER LOUPE
x Add 1px STROKE COLOR (put in DRAW.cfg) around TEXT for RGB and hex colors
x DO NOT show color picker when hovering over any UX like menu, toolbox, layer panel, bins, drawer


## BINS (common for all bins)
x Add right click context menu
  x SELECT - Left Click (show as disabled for hint)
  x STORE - SHIFT+Left Click (show as disabled for hint)
  x CLEAR - SHIFT+Middle CLick (show as disabled for hint)
  x LOAD - SHIFT+Right Click (show as disabled for hint)
  x --- DIVIDER ---
  x-Load Drawer Set (.dset)
  x Save Drawer Set (.dset)
  x- Clear Drawer Set
  x --- DIVIDER ---
  x Explore Folder
x Modify input handling to suit the above if it's different.

### EXTRA context menu options for PATTERNS (at the top)
x SOURCE = CLIPBOARD (show as disabled for hint)
x --- DIVIDER ---

### EXTRA context menu options for GRADIENTS (at the top)
x EDIT = Double Click (show as disabled for hint)

### EXTRA context menu options for BRUSHES (at the top)
x SOURCE = CLIPBOARD or CUSTOM BRUSH (show as disabled for hint)


## DRAWING MODES IN MENU
x Place at the top of the TOOLS menu:
x Tools:
  x DRAWING MODE: COLOR - F1
  x DRAWING MODE: GRADIENTS - F2
  x DRAWING MODE: PATTERNS- F3
  x --- DIVIDER ---
  x ... remaining tool options ....


## PREVIEW WINDOW
x A resizable preview window:
  x That sits on top of UI
  x Preview window can be resized and moved around
  x Preview window state is remembered across sessions:
  x [x] Follow pointer as a universal magnifier
    x X, Y position
    x W, H of preview window
    x Pan position of preview
    x Zoom level of preview
    x Open/closed state of preview window    
  x Buttons in window title bar:
    x X CLOSE
    x _ MINIMIZE
  x Under preview window:
    x [x] Follow pointer
      - Automatically pans to have center of preview window directly where
        mouse X,Y pointer is when enabled
  x Can be panned / zoomed independently
    x Pan: click and drag on preview image
      x When mouse over preview window, show hand cursor to indicate pan
    x Zoom: when over preview image wheel
  x When resizing window, use arrow pointers and corner pointers
x Menu addition: 
  x Position directly under View -> Color Strip/Status
  x View:
    x Preview Window [x] (F4 hotkey)
x Preview Window should auto hide when dragged over while drawing and redisplay when done drawing
x Preview Window should be included in View: Hide/Show All as part of "All"


## EDIT BAR
x Bar that contains quick icons to click from the EDIT menu (except transform submenu)
x Bar can be configured for placement: 
  x LEFT adjacent to the LAYERS PANEL
  x RIGHT adjacent to the TOOLBOX / DRAWER / BINS
x DRAW.cfg entry for:
  x EDIT_BAR_DOCK_POSITION
    x LEFT
    x RIGHT
x Bar icons are layed out in same order as EDIT menu (omit transform and transform submenu items)
x Where there is a menu divider, add vertical space to create margin/space between icons in bar
  x USE EDIT_BAR_DIVIDER_VERT_MARGIN for this amount of space
x When a icon is hovered over, or clicked on
  x Show a square rect outline around it in color: EDIT_BAR_ICON_MOUSE_OUTLINE_COLOR
    x 16x16 rect (or extents of the image boundaries for the icon)
x When an icon is clicked, it should engage the program the same way as if clicking the edit menu bar item
x Edit Bar should auto hide when dragged over while drawing and redisplay when done drawing
x Edit Bar should be included in View: Hide/Show All as part of "All"
x Menu addition: 
  x Position directly under View -> Color Strip/Status
  x View:
    x EDIT BAR [x] (F5 hotkey)
x Images for Edit Bar icons are in THEME/IMAGES/EDITBAR
x THEME.cfg entries for:
  x EDIT_BAR_LEFT_BORDER_COLOR
  x EDIT_BAR_RIGHT_BORDER_COLOR
  x EDIT_BAR_LEFT_BORDER_WIDTH
  x EDIT_BAR_RIGHT_BORDER_WIDTH
  x EDIT_BAR_WIDTH
  x EDIT_BAR_ICON_PADDING (default to 0)
  x EDIT_BAR_BG_COLOR
    x SPAN entire height of the program window from start position at bottom of menu
      x to bottom of program window
  x EDIT_BAR_ICON_MOUSE_OUTLINE_COLOR
  x EDIT_BAR_DIVIDER_VERT_MARGIN (this is vertical space between groups)
  x EDIT_BAR_ICON_UNDO
  x EDIT_BAR_ICON_REDO
  x EDIT_BAR_ICON_CUT
  x EDIT_BAR_ICON_COPY
  x EDIT_BAR_ICON_COPY_MERGED
  x EDIT_BAR_ICON_PASTE
  x EDIT_BAR_ICON_PASTE_IN_PLACE
  x EDIT_BAR_ICON_CUT_TO_LAYER
  x EDIT_BAR_ICON_COPY_TO_LAYER
  x EDIT_BAR_ICON_CLEAR
  x EDIT_BAR_ICON_FILL_WITH_FG
  x EDIT_BAR_ICON_FILL_WITH_BG
  x EDIT_BAR_ICON_STROKE_SELECTION
  x EDIT_BAR_ICON_FLIP_HORIZONTAL
  x EDIT_BAR_ICON_FLIP_VERTICAL
  x EDIT_BAR_ICON_SCALE_DOWN
  x EDIT_BAR_ICON_SCALE_UP
  x EDIT_BAR_ICON_ROTATE_CW
  x EDIT_BAR_ICON_ROTATE_CCW



## CANVAS SIZE SEPARATE FROM WINDOW SIZE

> Currently the canvas size just fills the entire window, no matter what.

x Create a new command line parameter --canvas-size WWWxHHH
x Center the canvas within the window on init
x When view -> scaling do not alter the canvas-size, just the window size
x Create new DRAW.cfg for DEFAULT_CANVAS_SIZE_W, DEFAULT_CANVAS_SIZE_H
x Set the default to 512x512
x When double clicking the hand tool or using view -> 100%, center the canvas in the window, as well
x When zooming the canvas and panning the canvas, it can be anywhere just like now
x When saving the file, it should save the file at the canvas-size
x When select -> all it should select all of the canvas
x When snap to grid is enabled and grids exist, they should work with their 0,0 at the top/left of the canvas
x When using the crosshair assistant or showing any x,y it should be relative to the 0,0 point of the canvas
x When creating a new document prompt for WxH like we do for Image -> Resize...
x When creating from template, do not prompt for WxH - use template images WxH




## LAYERS AND TOOLBOX POSITIONS
x Allow the layers to be on any edge of the screen
  x DRAW.cfg setting LAYERS_PANEL_DOCK_EDGE (left | right)
  x Default left
x Allow the toolbox to be on any edge of the screen
  x DRAW.cfg setting TOOLBOX_DOCK_EDGE (left | right)
  x Default right

### LAYERS AND TOOLBOX AND EDITBAR POSITIONS x SIDE of SCREEN PRIORITIES

#### LAYOUT GRID
The following configurations should be possible:

```
WHERE:
 L = LAYER PANEL
 T = TOOLBOX
 E = EDITBAR
 C = CANVAS / EDIT AREA
     (NOTE: CANVAS / EIDIT AREA spans whatever is left)

L C C T
T C C L
L T C C
C C T L
L C E T <-x CURRENT HARD CODED CONFIGURATION
T C E L
C E T L
L E C T
T E C L
L T E C
C C T L
```

#### LAYOUT GRID EXPLAINED:
x IF there are mutliple LEFT or RIGHT configurations in DRAW.cfg layout election happens like this:
  x LAYERS should always be horizontally closest to side of screen
  x TOOLBOX should always be closest to side of screen
    x UNLESS LAYERS is also on same side, in which case LAYERS should be closest to side of screen
  x EDITBAR should always be horizontally adjacent to LAYERS or TOOLBOX depending on priority
    x If EDITBAR is visible should be adjacent to whichever UX is closest to side of screen  
x This should allow the following configurations:
  x LAYERS on LEFT, EDITBAR on RIGHT, TOOLBOX on RIGHT (current hard-coded configuration)
  x LAYERS on LEFT, EDITBAR on LEFT, TOOLBOX on RIGHT (adjacent to LAYERS)
  x LAYERS on LEFT, EDITBAR on LEFT, TOOLBOX on LEFT
  x LAYERS on RIGHT, TOOLBOX on RIGHT, EDITBAR on RIGHT
  x LAYERS on RIGHT, TOOLBOX on RIGHT, EDITBAR on LEFT
  x LAYERS on RIGHT, TOOLBOX on LEFT, EDITBAR on RIGHT
  x LAYERS on RIGHT, TOOLBOX on LEFT, EDITBAR on LEFT

#### NEW SHORTCUTS FOR HIDING UX BY SCREEN SIDE:
x CTRL+ALT+SHIFT+Up arrow = Hide/Show Menu bar
x CTRL+ALT+SHIFT+Down arrow = Hide/Show Status bar/Color strip
x CTRL+ALT+SHIFT+Left arrow = Hide/Show ANYTHING stacked on left side of screen
x CTRL+ALT+SHIFT+Right arrow = Hide/Show ANYTHING stacked on right side of screen
x These do not need presence in the menu, only in the COMMAND palette


## PATTERN TILE MODE
x View:
  x --x DIVIDER ---
  x [x] PATTERN TILE MODE (<= 512px) ... SHIFT+TAB
    x Only available if canvas size is <= 512 x 512
    x If canvas is > 512px in any dimension x show as grayed out/disabled
    x Checkable option x OFF by default
    x Checked state IS SAVED with file for later edits

### PATTERN TILE MODE FUNCTION
x ON: 
  x Canvas content repeats one time in every direction and diagonals: 

    ```
    x Where [C] is cloned copy of [S]OURCE 
    x [C] regions are not part of the actual canvas in the file, etc.
    x They are only shown and active when View -> PATTERN TILE MODE is active.
    x The are rendered and laid out in a 3x3 grid to assist in the creation of patterns and tiles.

    3x3 grid:

    C C C
    C S C
    C C C

    S = SOURCE canvas (actually part of the file when saved, etc.)
    C = CLONED canvas
    ```

  x All regions and clones are editable and apply edits to the [S]
  x When [S] changes, update in real time [C]
  x Show cursors, assistants, etc. in [C] just as you do [S] but:
    x Do not have duplicate cursors or assistants.
    x For example if the user is painting on a [C], show the same cursors/UX
      as when they are painting on [S], allow same functions.
    x If the user is using color picker on [C], show same cursor/UX, etc.
  x SELECTIONS should not be rendered more than once. 

x OFF:
  x Normal mode as it is now. 

