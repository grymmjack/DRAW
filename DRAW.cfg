# DRAW Configuration File
# Auto-generated by DRAW
# Edit values as needed, changes will be loaded on next run

# Theme name (default: DEFAULT)
THEME=DEFAULT

# Fullscreen mode (1=true, 0=false)
FULLSCREEN=-1

# Screen dimensions (0=auto-detect desktop resolution)
SCREEN_WIDTH=320
SCREEN_HEIGHT=200

# Default palette file (empty=use built-in default)
DEFAULT_PALETTE=

# Nudge amount for arrow key movements
NUDGE_N=10

# Number of custom brush slots
NUM_CUSTOM_BRUSHES=4

# Number of layers
NUM_LAYERS=1

# FPS limit
FPS_LIMIT=60

# Palette strip chip dimensions (width x height in pixels)
# Valid range: 6-32 pixels (default: 10x10)
PALETTE_CHIP_WIDTH=10
PALETTE_CHIP_HEIGHT=10

# Palette strip layout
# Max chips per row: 4-128 (default: 64)
PALETTE_MAX_CHIPS_PER_ROW=16

# Max rows: 1-8 (default: 3)
PALETTE_MAX_ROWS=3

# Min rows to always display: 1-8 (default: 1)
PALETTE_MIN_ROWS=1

# Create new row every N chips (default: 16)
# Example: 16 means 16 chips = 1 row, 17-32 = 2 rows, 33-48 = 3 rows
PALETTE_CHIPS_ROW_THRESHOLD=16
